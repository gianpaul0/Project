<h1>Listing courses</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% current_admin.courses.each do |c| %>
      <tr>
	<td><li><%= link_to c.name, c %></td></li>
        <td><%= link_to 'Remove', remove_course_path(c) %></td>
        <!-- <td><%= link_to 'Show', c %></td>
        <td><%= link_to 'Edit', edit_course_path(c) %></td>
        <td><%= link_to 'Destroy', c, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<strong><%= link_to 'Add Course', new_course_path %></strong>
